# 🔧 i18n 错误修复

## 问题
```
NameError: name 'i18n' is not defined
```

## 原因
在 `_calculate_real_performance` 方法中使用了 `i18n.t()`，但该方法没有接收 `i18n` 参数。

## 修复
移除了所有内部方法中对 `i18n` 的引用，改用英文硬编码的错误消息。

### 修改的代码

**之前**：
```python
st.warning(f"⚠️ {i18n.t('warning')}: {symbol} - 返回的数据为空")
st.error(f"❌ {i18n.t('error')}: 获取 {symbol} 数据时出错")
```

**之后**：
```python
st.warning(f"⚠️ Warning: {symbol} - Returned data is empty")
st.error(f"❌ Error: Failed to fetch {symbol} data")
```

## 测试
重新启动仪表板，测试加密货币回测功能：

```bash
cd D:\sheldonproject\QuantMuse1
python run_dashboard.py
```

1. 选择 Cryptocurrency
2. 输入 bitcoin
3. 运行回测

应该可以正常工作了！
